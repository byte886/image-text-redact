---
name: image-text-redact
description: 给截图、长图、Markdown/文本里的敏感信息脱敏/打码后再对外分享。当用户要把图或文字里的手机号、身份证、银行卡、邮箱、姓名、地址或自定义敏感词糊掉/抹掉/打码/脱敏/发布前脱敏/长图敏感词替换时使用。首选对源文本做等长 █ 替换再重绘（零残字、不误伤邻字）；只有位图无源文本时才用 macOS Vision OCR 定位+色块兜底。内置手机号/身份证/银行卡/邮箱/IP 等 PII 正则，支持自定义词表，可渲染脱敏长图 PNG。
compatibility: 仅在 macOS(Darwin) 实测可用；Windows/Linux 未适配。执行前先判平台(uname -s 返回 Darwin)，非 macOS 停止并告知需另行适配、不硬跑；将来补齐 Windows 后仍按平台分流并分别标注验证状态
---

# 图文 / 长图文字脱敏

核心原则：**能拿到源文本（MD/TXT）就走"等长替换 + 重绘"，绝不直接在位图上糊**——直接打码易残留半个字、误伤相邻字（已验证的坑）。只有拿不到源文本、只有一张位图时，才用 OCR 兜底，并在交付前放大复核。

## 平台适用（执行前先读）
- 本技能当前**仅在 macOS（Darwin）实测可用**，命令、路径与系统原生能力均按 Mac。
- 动手前先判平台：`uname -s` 返回 `Darwin` 才走本技能流程；**Windows/Linux 未适配，遇到就停下告知用户"需先做该平台适配"，不要用想当然的等价命令硬跑**。
- 以后补齐 Windows 后也必须保留"先判平台 → 按平台分流"的结构：mac/Windows 的命令与路径分开写、各自标注是否已验证。

## 路线怎么选

- **路线 A（首选）**：有源 MD/TXT → `redact.py` 等长 █ 替换，可 `--png` 重绘长图。
- **路线 B（兜底）**：只有位图 → `--image` 走 Vision OCR 定位 + 色块，交付前放大逐行复核。

## 按需加载索引

| 你要做什么 | 读这篇 |
|---|---|
| 路线 A/B 命令与旗标（--word/--char/--png/--width、PILLOW_PYTHON）、IPv4 版本号歧义、操作流程 | [references/usage.md](references/usage.md) |
| 敏感词分类词库模板与扩词方法 | [references/sensitive-words.md](references/sensitive-words.md) |

## 关键约束（AI 即时行为红线）

- 等长替换按"区间合并"实现，组合词、重叠 PII 不会重复替换、不会错位；长词务必排在短词前（脚本已按长度自动排序）。
- PII 正则刻意保守：年份（如 2026）、短数字（123）、普通金额不会被误遮；QQ/微信号等易误伤项不自动遮，需要就写进词表。IPv4 只遮每段 0–255 的独立四段；唯一无法消歧的"独立合法四段版本号"见 usage.md，遇到加 `v` 前缀或人工核对。
- 敏感词高度场景化，没有"万能词表"：通用隐私靠内置 PII，主题词按 [sensitive-words.md](references/sensitive-words.md) 分类模板定制。
- 渲染依赖本机 Chrome/Edge；找不到浏览器时文本脱敏文件照常产出，只是跳过 PNG。
- 交付前对照原文逐项核对：该糊的全糊、不该糊的保留（防中性词被连带误伤）。
